{
  "name": "cc-cloudsave-mod",
  "version": "0.0.1",
  "scripts": {
    "build": "tsc --build",
    "watch": "tsc --watch",
    "lint": "eslint . --ext .js,.ts --ignore-path .gitignore"
  },
  "dependencies": {
    "request": "^2.88.2"
  },
  "devDependencies": {
    "eslint": "^8.3.0",
    "typescript": "^4.5.2",
    "ultimate-crosscode-typedefs": "dmitmel/ultimate-crosscode-typedefs"
  }
}
